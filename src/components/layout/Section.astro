---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"section">;

const { class: className, ...rest } = Astro.props;
---

<section class:list={["w-full", className]} {...rest}>
	<slot />
</section>
